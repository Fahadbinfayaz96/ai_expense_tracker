import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF1A1A2E);
  static const secondary = Color(0xFF565D87);

  // Light
  static const lightBackground = Color(0xFFF8F9FA);
  static const lightSurface = Colors.white;

  static const lightTextPrimary = Color(0xFF1A1A2E);
  static const lightTextSecondary = Color(0xFF6C757D);

  // Dark
  static const darkBackground = Color(0xFF0F0F14);
  static const darkSurface = Color(0xFF1C1C27);

  static const darkTextPrimary = Color(0xFFF1F1F5);
  static const darkTextSecondary = Color(0xFFA0A0B8);

  // Status
  static const success = Color(0xFF27AE60);
  static const error = Color(0xFFEB5757);
  static const warning = Color(0xFFF2994A);
}
